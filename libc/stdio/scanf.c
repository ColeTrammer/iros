#include <stdio.h>
#include <assert.h>
#include <stdbool.h>

int sscanf(const char *__restrict str, const char *__restrict format, ...) {
    (void) str;
    (void) format;

    assert(false);
    return 0;
}
#pragma once

#include <di/util/types/in_place.h>
#include <di/util/types/in_place_index.h>
#include <di/util/types/in_place_type.h>
#include <di/util/types/integers.h>
#include <di/util/types/nullptr_t.h>
#include <di/util/types/partial_ordering.h>
#include <di/util/types/piecewise_construct.h>
#include <di/util/types/ptrdiff_t.h>
#include <di/util/types/size_t.h>
#include <di/util/types/strong_ordering.h>
#include <di/util/types/weak_ordering.h>

namespace di::util {
using types::i16;
using types::i32;
using types::i64;
using types::i8;
using types::in_place;
using types::in_place_index;
using types::in_place_type;
using types::InPlace;
using types::InPlaceIndex;
using types::InPlaceType;
using types::nullptr_t;
using types::partial_ordering;
using types::piecewise_construct;
using types::PiecewiseConstruct;
using types::ptrdiff_t;
using types::size_t;
using types::strong_ordering;
using types::u16;
using types::u32;
using types::u64;
using types::u8;
using types::weak_ordering;
}

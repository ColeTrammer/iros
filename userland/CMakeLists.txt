if (NOT ${NATIVE_BUILD})
    set(OS_TARGETS
        chown
        ping
        stat
        top
        xterm
    )
endif()

set(TARGETS
    ${OS_TARGETS}
    basename
    cat
    chmod
    clear
    cmp
    comm
    cp
    cut
    date
    dirname
    echo
    edit
    env
    false
    grep
    head
    id
    kill
    ln
    ls
    mkdir
    mv
    nslookup
    paste
    ps
    pwd
    realpath
    rm
    rmdir
    sh
    sleep
    sort
    start
    su
    tail
    tee
    touch
    true
    uname
    uniq
    which
    window_server
    yes
)

add_all_targets()
set(USERLAND_TARGETS ${TARGETS} PARENT_SCOPE)
if (NOT ${NATIVE_BUILD})
    set(OS_TARGETS
        chown
        ping
        sh
        stat
        top
        xterm
    )
endif()

set(TARGETS
    ${OS_TARGETS}
    basename
    cat
    chmod
    clear
    cp
    date
    dirname
    echo
    env
    false
    grep
    head
    id
    kill
    ln
    ls
    mkdir
    mv
    nslookup
    ps
    pwd
    realpath
    rm
    rmdir
    sleep
    start
    su
    tail
    touch
    true
    uname
    window_server
    yes
)

add_all_targets()
set(USERLAND_TARGETS ${TARGETS} PARENT_SCOPE)